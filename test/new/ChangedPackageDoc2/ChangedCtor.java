package ChangedPackageDoc2;

/** 
 * OLD: The changes in this class are only in documentation.
 */
public class ChangedCtor {

    /** NEW This constructor should remain unchanged, but this comment changes. */
    public ChangedCtor() {
    }

    /** OLD This method should remain unchanged. */
    public void UnchangedMethod() {
    }

    /** OLD This field should remain unchanged. */
    public boolean unchanged;
}
